import classNames from "classnames";

const Header = () => {
  return (
    <div
      className={classNames(
        "border-b border-gray-100",
        "py-3 px-12"
      )}
    >
      Header
    </div>
  );
};

export default Header;
